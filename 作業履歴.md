# 作業履歴

## 2026-03-11: チャンピオン結果集計のリセット
- cloudflare-worker/worker.js: POST /reset エンドポイントを追加（KVの全集計データをリセットする機能）
- Workerをデプロイし、/reset を実行して集計データをリセット

## 2026-03-11: Workerにエラーハンドリング追加
- cloudflare-worker/worker.js: 全体をtry-catchで囲み、エラー時もCORSヘッダー付きレスポンスを返すよう修正

## 2026-03-11: サモナータイプのプロファイルをリバランス
- js/data.js: 7タイプのprofile値を調整、万能の適応者のprofileも微調整
- js/diagnosis.js: 万能の適応者のペナルティを1800→1000に軽減
- チャンピオンのスコアは変更なし（診断結果の満足度に影響させない）
- タイプ別チャンピオン数: 0〜34個 → 10〜16個 に均等化
- cloudflare-worker/worker.js: /rankings/all エンドポイントを追加（全データ取得用）

## 2026-03-10: テストモードにバッチ自動診断機能を追加
- js/main.js: `?test=100`のようにURLパラメータで回数指定すると、自動で複数回診断を実行しサーバーにデータ送信する機能を追加
- ロール選択もランダム化（ANY固定→全ロールからランダム）

## 2026-03-10: ダウンロードボタン押下時のテキスト変更を廃止
- js/export.js: ダウンロード中に文言を変更していた処理を削除し、ボタンを無効化（暗くなる）するだけにした

## 2026-03-10: ランキング表示が自分の診断結果と一致しない問題を修正
- cloudflare-worker/worker.js: POST /record のレスポンスに書き込み直後のランキングデータを含めるよう変更（KV eventual consistency対策）
- js/main.js: sendDiagnosisResult がレスポンスからランキングを直接描画するよう変更、デバッグ用console.logを追加
- js/result.js: sendDiagnosisResult完了後にloadRankingsを呼ぶようチェーン化（送信前に古いランキングが表示される問題の修正）

## 2026-03-10: プライバシーポリシーをCloudflare移行後の内容に更新
- privacy.html: 診断結果の保存先をFirebase → Cloudflare Workers KVに変更
- privacy.html: 第三者サービス一覧からFirebase・reCAPTCHAを削除、Cloudflareを追加
- privacy.html: Cookieセクションを「使用していない」旨に変更
- privacy.html: 最終更新日にrev.2を追記

## 2026-03-10: ランキングバックエンドを Firebase から Cloudflare Workers + KV に移行
- cloudflare-worker/worker.js: 新規作成。POST /record（診断記録）・GET /rankings（ランキング取得）を実装
- cloudflare-worker/wrangler.toml: 新規作成。Worker デプロイ設定
- js/worker-config.js: 新規作成。Worker URL 設定ファイル
- js/main.js: sendDiagnosisResult・loadRankings を Firebase SDK から fetch（Worker API）呼び出しに変更
- index.html: Firebase SDK スクリプト3本を削除、firebase-config.js・firebase.js を除外、worker-config.js を追加
- js/firebase.js: 削除
- js/firebase-config.js: 削除（機密情報を含むため）
- js/firebase-config.example.js: 削除
- .gitignore: firebase-config.js の除外設定を削除し、node_modules/・.wrangler/ に変更

## 2026-03-10: Firebase App Check（reCAPTCHA v3）を実装
- index.html: firebase-app-check-compat.js を追加
- js/firebase.js: App Check を初期化（activate）してから db を生成するよう変更

## 2026-03-10: プライバシーポリシーに Google reCAPTCHA v3 の記載を追加
- privacy.html: 第三者サービス表に reCAPTCHA v3 を追加（JP・EN）
- privacy.html: Cookie セクションを reCAPTCHA の使用に合わせて更新（JP・EN）
- privacy.html: 最終更新日を 2026-03-10 に更新

## 2026-03-10: フッターにお問い合わせリンクを追加
- index.html: フッターのプライバシーポリシーリンク横に「お問い合わせ / Contact」リンクを追加

## 2026-03-10: お問い合わせページを作成
- contact.html: 新規作成。Google フォームへのリンクボタンを設置。日英2言語対応。デザインは privacy.html に合わせたLoLテーマ

## 2026-03-10: ダウンロードボタン押下時のテキスト変更を廃止
- js/export.js: ダウンロード中に「生成中…」へテキスト変更していた処理を削除し、テキストを変えずボタンを無効化するだけにした（2行になる問題の修正）

## 2026-03-10: ロール選択画面の「どこでも」アイコンを👑からfill.pngに変更
- index.html: ANYカードの `<span>👑</span>` → `<img src="role_icons/fill.png">` に変更

## 2026-03-10: ロールランキングにANY（どこでも）を追加・集計対応
- js/main.js: `sendDiagnosisResult` でANYをそのまま保存するよう変更（`getBestLane()`変換を廃止）
- js/main.js: ランキング表示に `fill.png` アイコン・「どこでも」ラベルを追加、上限を6に変更
- index.html: タイトルを「人気ロール TOP5」→「人気ロール ランキング」に変更

## 2026-03-10: ロールランキングをTOP3からTOP5（全5ロール）表示に変更
- js/main.js: `roles:top(roleCount,3)` → `top(roleCount,5)` に変更
- index.html: タイトルを「人気ロール TOP3」→「人気ロール TOP5」に変更

## 2026-03-10: 「対戦後の印象」「強み・弱み」を1段階目から削除し2段階目のみ表示に変更
- result.js: `sec-perception`・`sec-sw` を `is16`（1段階目）から `is32`（2段階目）の表示グループに移動
- export.js: ダウンロードカードの強み・弱み（`ce-sw-row`）を `is40`（2段階目）のみ表示に変更
- index.html: HTMLコメントを「16問+」→「32問+」に更新・`ce-sw-row` に ID を付与

## 2026-03-09: 結果カードのダウンロード仕様変更（2段階構造に対応）
- export.js: `is25` の閾値を `>=25` → `>=16` に変更（1段階目でサモナータイプを表示）
- export.js: `is40` の閾値を `>=40` → `>=32` に変更（2段階目でレーダー＋バトルクリードを表示）

## 2026-03-09: 「もう一度診断する」「トップページに戻る」ボタンの間隔を縮小
- index.html: 2ボタンを `.result-actions-sub` でグループ化
- style.css: `.result-actions-sub` に `gap:8px` を設定

## 2026-03-09: 「さらに答えて」ボタンのアニメーションを縮小
- style.css: `pulse-btn` の scale を `1.08` → `1.05` に変更

## 2026-03-09: 「さらに答えて」ボタンの横幅を縮小
- style.css: `#btn-add-diagnosis` の padding を `20px 60px` → `20px 32px` に変更

## 2026-03-09: ボタン文言を変更
- index.html, ui.js, export.js: 「結果カードをダウンロードする」→「結果カードをダウンロード」、「Xでシェアする」→「ポストする」

## 2026-03-09: ダウンロード・シェアボタンの横幅を統一
- style.css: `.btn-download` `.btn-share-x` に `width:260px; justify-content:center` を追加

## 2026-03-09: スマホでの「さらに答えて精度を上げる」ボタンが2行になる問題を修正
- style.css: `#btn-add-diagnosis` にスマホ用メディアクエリを追加（font-size縮小・padding縮小・white-space:nowrap）

## 2026-03-09: スマホでの選択肢ホバー貼り付き問題を修正
- style.css: `.choice-btn:hover` を `@media(hover:hover)` でラップし、タッチデバイスでのスティッキーホバーを防止

## 2026-03-09: 相性・天敵チャンピオンの表示数を変更
- result.js: allies・enemies の表示を slice(0,4) で5体→4体に変更

## 2026-03-09: 前回の診断結果バナーの表示位置を変更
- index.html: last-result-banner をボタンの上から下に移動
- style.css: last-result-banner の margin-bottom → margin-top に変更

## 2026-03-09: スマホ時の診断を始めるボタンをさらに拡大
- style.css: max-width 500px 以下で #btn-start の padding を 16px 36px、font-size を 16px に設定

## 2026-03-09: 診断を始めるボタンのサイズを拡大
- style.css: .btn-primary の padding を 11px 20px → 14px 32px、font-size を 14px → 15px に変更

## 2026-03-09: 診断ボタンのサイズを縮小
- style.css: .btn-primary の padding を 16px 48px → 11px 20px、font-size を 15px → 14px に変更

## 2026-03-09: スタート画面のCTAボタン優先順位を改善
- style.css: 「サモナー素質診断とは？」ボタンをゴールドグラデーションからアイコン付きテキストリンクスタイルに変更
- index.html: 同ボタンのテキストを「サモナー素質診断とは」に修正（?アイコンはCSSで付与）

## 2026-03-09: テストモードを改修
- js/main.js: テストモード（`?test=1`）の仕様を変更
  - スタートページ → 診断開始で1段階目の結果（phase1自動回答）→「さらに答えて精度を上げる」で2段階目の結果（phase2自動回答）という3段階フローに変更
  - 広告オーバーレイとローディング画面もスキップ

## 2026-03-09: デザイン改善報告書を作成
- デザイン改善報告書_20260309.md を新規作成
- コードベース静的分析にもとづく10項目の改善提案をまとめた

## 2026-03-09: ロールタブをページ上部に復元
- index.html: result-header直下にlane-tabs（TOP/JUNGLE/MID/ADC/SUPPORT）を復元、lane-display（トグルUI）を削除
- style.css: lane-tabsのCSSを旧スタイルに戻し、lane-display/lane-toggle-btn/lane-others関連スタイルを削除
- js/result.js: lane-display→lane-tabsに変更
- js/ui.js: renderLaneTabs を全タブ表示方式に戻し、toggleOtherLanes を削除

## 2026-03-08: みんなの診断結果の絵文字削除・ロールランキング画像化
- index.html / data.js / ui.js / export.js: ラベル・タイトルから絵文字（🤝⚔️🏆🗺️🎮⬡⬇など）を削除
- main.js: ロールランキングのロールアイコンを絵文字→role_icons/*.png の画像に変更
- result.js: 未使用の roleIcons オブジェクト（絵文字）を削除
- style.css: .rank-role-icon スタイルを追加

## 2026-03-08: 結果コンテンツ表示条件を新フェーズ構成に合わせて更新
- result.js: is25/is40の閾値をis16(>=16)/is32(>=32)に変更
- 16問終了後にSummoner Type・Battle Creed・After Game Chat・Strengths & Weaknessesを表示
- 32問終了後にSynergy・Shadow Self・Soul Resonance・Natural Enemy・Detailed Analysis・Community Statsを表示
- 「さらに答えて精度を上げる」ボタンの表示条件をcurrentPhaseEnd<32に変更
- index.html: セクションコメントの問数表記を更新（25問+→16問+、40問+/50問→32問+）

## 2026-03-08: フェーズ構成変更（16問×2フェーズ）
- PHASE_OFFSET/PHASE_LENGTHを{1:16,2:16}に変更（旧：10/15/15の3フェーズ）
- Phase 3を廃止し、2フェーズ構成（計32問）に変更
- 各次元が2問ずつ均等に配分されグラフの極端な値を緩和

## 2026-03-08: アクセシビリティ・パフォーマンス・CSS改善（低優先度）
- html2canvasをダウンロードボタン押下時の動的読み込みに変更（初期ロード軽量化）
- about-modalにrole=dialog/aria-modal/aria-label追加（アクセシビリティ対応）
- モーダル・広告クローズボタンにaria-label追加
- 装飾SVG（外部リンクアイコン、Xアイコン）にaria-hidden=true追加
- style.css: 未使用セレクタ3件削除（.qcount-right, .ext-label-placeholder, .axis-row-right）
- style.css: タブレット用ブレークポイント追加（768px時にroles-gridを4列表示）

## 2026-03-08: コード品質改善（重要度：高）
- drawRadarChart()共通関数を作成しui.js/export.jsのレーダー描画重複を解消
- main.jsにresetState()を抽出し、startDiagnosis/retryDiagnosis/goToTopの重複コードを統合
- retryDiagnosis()をstartDiagnosis()呼び出しに簡略化
- フェーズ定数PHASE_OFFSET/PHASE_LENGTHをmain.jsに追加し、ui.js/diagnosis.jsのマジックナンバーを置き換え
- vars.cssにCSSカスタムプロパティを分離し、style.css/privacy.htmlの:root重複を解消

## 2026-03-08: セキュリティ・バグ修正（緊急度高）
- Firebase APIキーをfirebase-config.jsに分離、.gitignoreで除外（firebase-config.example.jsをテンプレートとして追加）
- storage.js: catch(e){}のエラー握りつぶしを修正（console.error＋破損データ削除）
- export.js: 画像onload/onerror競合バグを修正（cleanup関数で二重呼び出し防止）
- .gitignore新規作成

## 2026-03-07: index.htmlの複数ファイル分割
- CSSをstyle.cssに分離（427行）
- JavaScriptを機能単位に8ファイルに分割:
  - js/data.js: データ定数（CHAMPIONS, QUESTION_DATA等）
  - js/firebase.js: Firebase初期化・設定
  - js/storage.js: LocalStorage操作
  - js/diagnosis.js: 診断ロジック
  - js/ui.js: UI描画・画面遷移
  - js/result.js: 結果表示・レーン切替
  - js/export.js: カードダウンロード・Xシェア
  - js/main.js: グローバルstate・イベント初期化
- index.htmlは2699行→489行に縮小
